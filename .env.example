# Set your Anthropic API key here. Get one at https://console.anthropic.com/
# If you don't set this server-side, the app will let you paste a key in the UI
# (stored only in your browser's localStorage) and forward it on each request.
ANTHROPIC_API_KEY=
